# ErrorState (Enmarket UI)

- **Framework**: React / Next.js (TypeScript TSX)
- **Sumber Referensi**: `enmarket` (`apps/web/components/ui/ErrorState.tsx`)
- **File Target**: `component.tsx`

## Deskripsi
Komponen referensi `ErrorState` berbasis React/Next.js dan Tailwind CSS (Neobrutalism/Enmarket UI).

## Cara Penggunaan (Copy-Paste)
1. Salin `component.tsx` ke direktori komponen aplikasi Next.js/React Anda.
2. Sesuaikan tokens/class styling agar match dengan tema aplikasi tujuan.

## Props
```typescript
eyebrow: string;
title: string;
description: string;
actions?: ReactNode;
children?: ReactNode;
className?: string;
eyebrowColor?: 'primary' | 'muted';
titleClassName?: string;
descriptionClassName?: string;
```
